import { RequestResult } from "./components/RequestResult";
import { RequestRoleTranslator } from "./components/RequestRoleTranslator";

const RequestPage = () => {
  return (
    <div>
      <RequestRoleTranslator />
    </div>
  );
};

export default RequestPage;
